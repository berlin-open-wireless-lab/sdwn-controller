<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<features xmlns="http://karaf.apache.org/xmlns/features/v1.2.0" name="${project.artifactId}-${project.version}">
    <feature name="${project.artifactId}" version="${project.version}"
             description="${project.description}">
        <feature>onos-api</feature>
        <bundle>mvn:de.tuberlin.inet.sdwn/openflowj/3.1.0.sdwn-SNAPSHOT</bundle>
        <bundle>mvn:${project.groupId}/sdwn-core-api/${project.version}</bundle>
        <bundle>mvn:${project.groupId}/sdwn-core-ctl/${project.version}</bundle>

        <bundle>mvn:${project.groupId}/sdwn-core-cli/${project.version}</bundle>
        <bundle>mvn:${project.groupId}/sdwn-core-rest/${project.version}</bundle>
    </feature>
</features>
